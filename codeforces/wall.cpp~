#include <iostream>

using namespace std;

int main ( )
{
  long int x, y, a, b, cont = 0;

  cin >> x >> y >> a >> b;
  
  for (; a <= b; a++) if (a % x == 0 && b % y == 0) cont++;
  
  cout << cont;

  return 0;
}
